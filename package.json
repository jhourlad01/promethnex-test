{
  "name": "product-catalog-frontend",
  "version": "1.0.0",
  "description": "Promethex Store Frontend",
  "scripts": {
    "watch": "sass assets/scss/styles.scss assets/css/style.css --watch",
    "build": "sass assets/scss/styles.scss assets/css/style.css --style=compressed",
    "lint:js": "eslint assets/js/*.js --config config/.eslintrc.js",
    "lint:php": "vendor\\bin\\phpcs --standard=config/phpcs.xml",
    "lint": "npm run lint:js && npm run lint:php",
    "seed": "php db/seeder.php",
    "analyze": "node scripts/playwright/analyze.js",
    "analyze-simple": "node scripts/playwright/simple-analyze.js",
    "test-models": "node scripts/playwright/test-model-download.js",
    "prestart": "npm run lint && echo 'All checks passed!'",
    "start": "npm run prestart && concurrently \"npm run watch\" \"browser-sync start --config config/browser-sync.config.js\" \"php -S localhost:8001\""
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.1",
    "eslint": "^8.57.1",
    "playwright": "^1.40.0",
    "prettier": "^3.6.2",
    "sass": "^1.69.5"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "canvas": "^3.2.0",
    "dotenv": "^17.2.2",
    "onnxruntime-node": "^1.23.0"
  }
}
